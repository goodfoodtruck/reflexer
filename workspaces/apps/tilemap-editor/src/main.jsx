import React from "react";
import ReactDOM from "react-dom/client";
import TilemapEditor from "./TilemapEditor.jsx";
import "./styles.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TilemapEditor />
  </React.StrictMode>
);
